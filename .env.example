# ------------------------------------------------------------------
# TECHSTEAL website — environment variables
# Copy this file to `.env.local` and fill in your values.
# On Vercel, set these in Project → Settings → Environment Variables.
# ------------------------------------------------------------------

# --- Database (required for live content) --------------------------
# Create a free Postgres instance on Neon (https://neon.tech), then paste
# the pooled connection string. Vercel Postgres / Supabase also work.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# --- Minecraft server status ----------------------------------------
# Address shown on the site and queried against mcsrvstat.us for live status.
MINECRAFT_SERVER="play.techsteal.gg"
MINECRAFT_PORT="25565"

# --- AI assistant (optional) ----------------------------------------
# Leave AI_API_KEY empty to run the assistant in "not connected" mode.
# Any OpenAI-compatible /chat/completions endpoint works.
AI_API_KEY=""
AI_BASE_URL="https://api.openai.com/v1"
AI_MODEL="gpt-4o-mini"

# --- App -------------------------------------------------------------
NEXT_PUBLIC_SITE_URL="https://your-project.vercel.app"
